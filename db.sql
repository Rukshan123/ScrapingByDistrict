create database if not exists ikman;

use ikman;

create table Adds (
    district varchar (100),
    category varchar (100),
    model varchar (100),
    price varchar (100),
    descr varchar (255)

)ENGINE=INNODB;
